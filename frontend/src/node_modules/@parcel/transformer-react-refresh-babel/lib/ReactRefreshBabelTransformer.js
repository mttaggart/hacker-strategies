"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function _plugin() {
  const data = require("@parcel/plugin");

  _plugin = function () {
    return data;
  };

  return data;
}

function _assert() {
  const data = _interopRequireDefault(require("assert"));

  _assert = function () {
    return data;
  };

  return data;
}

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

async function shouldExclude(config, options) {
  if (!config.isSource || !options.hmrOptions || !config.env.isBrowser() || options.mode !== 'development') {
    return true;
  } else {
    var _pkg$dependencies, _pkg$peerDependencies;

    let pkg = await config.getPackage();
    let usesReact = (pkg === null || pkg === void 0 ? void 0 : (_pkg$dependencies = pkg.dependencies) === null || _pkg$dependencies === void 0 ? void 0 : _pkg$dependencies.react) || (pkg === null || pkg === void 0 ? void 0 : (_pkg$peerDependencies = pkg.peerDependencies) === null || _pkg$peerDependencies === void 0 ? void 0 : _pkg$peerDependencies.react);
    return !usesReact;
  }
}

var _default = new (_plugin().Transformer)({
  async loadConfig({
    config,
    options
  }) {
    config.setResult(await shouldExclude(config, options));
  },

  transform({
    asset,
    config
  }) {
    if (!config) {
      asset.meta.babelPlugins = asset.meta.babelPlugins || [];
      (0, _assert().default)(Array.isArray(asset.meta.babelPlugins));
      asset.meta.babelPlugins.push([require.resolve('react-refresh/babel'), {
        skipEnvCheck: true
      }]);
    }

    return [asset];
  }

});

exports.default = _default;