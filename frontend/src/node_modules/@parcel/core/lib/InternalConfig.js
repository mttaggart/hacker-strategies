"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createConfig = createConfig;

function _utils() {
  const data = require("@parcel/utils");

  _utils = function () {
    return data;
  };

  return data;
}

function createConfig({
  plugin,
  isSource,
  searchPath,
  env,
  result,
  includedFiles,
  invalidateOnFileCreate,
  devDeps,
  shouldInvalidateOnStartup
}) {
  return {
    id: (0, _utils().md5FromString)(plugin + searchPath + env.id + String(isSource)),
    isSource,
    searchPath,
    env,
    result: result !== null && result !== void 0 ? result : null,
    resultHash: null,
    includedFiles: includedFiles !== null && includedFiles !== void 0 ? includedFiles : new Set(),
    invalidateOnFileCreate: invalidateOnFileCreate !== null && invalidateOnFileCreate !== void 0 ? invalidateOnFileCreate : [],
    devDeps: devDeps !== null && devDeps !== void 0 ? devDeps : [],
    shouldInvalidateOnStartup: shouldInvalidateOnStartup !== null && shouldInvalidateOnStartup !== void 0 ? shouldInvalidateOnStartup : false
  };
}